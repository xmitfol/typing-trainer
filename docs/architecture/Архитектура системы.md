**🏗****️ Системная архитектура - Общий обзор**
**Typing Trainer SaaS v2.0**
**Версия:** 1.0
**Дата:** 09 октября 2025
**Тип:** Архитектурный документ

**📋**** Содержание**









**🎯**** Архитектурные принципы**
**Ключевые принципы дизайна**
**1. Start Simple, Scale Smart**
Начинаем с монолита (MVP)
Готовимся к микросервисам (v2.0+)
Модульная структура с первого дня
**2. API-First**
Все взаимодействия через REST/GraphQL API
Готовность к мобильным приложениям
Возможность интеграций
**3. Data-Driven**
Детальная телеметрия с первого дня
Готовность к AI/ML
Аналитика встроена в архитектуру
**4. Cloud-Native**
Контейнеризация (Docker)
Stateless сервисы
Горизонтальное масштабирование
**5. Security by Design**
Безопасность на каждом уровне
Шифрование данных
GDPR/CCPA compliance
**6. Progressive Enhancement**
Работает без JavaScript (базово)
Улучшения для современных браузеров
Graceful degradation

**🔄**** Эволюция архитектуры**
**Фаза 1: MVP (Месяц 1-3)**
**Статус:** В разработке
**Архитектурный стиль:** Static Site + LocalStorage
┌────────────────────────────────────┐
│         User Browser               │
│  ┌──────────────────────────────┐ │
│  │   Static HTML/CSS/JS         │ │
│  │   - Vanilla JavaScript       │ │
│  │   - LocalStorage             │ │
│  │   - No backend required      │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
         │
         │ Deploy
         ▼
┌────────────────────────────────────┐
│   Static Hosting (Netlify/Vercel) │
│   - CDN распространение            │
│   - Automatic SSL                  │
│   - $0 cost                        │
└────────────────────────────────────┘
**Преимущества:**
✅ Быстрый запуск (недели)
✅ Нулевые расходы на инфраструктуру
✅ Высокая производительность (CDN)
✅ Простота разработки
**Ограничения:**
❌ Нет синхронизации между устройствами
❌ Нет аналитики в реальном времени
❌ Нет социальных функций
❌ Ограниченная персонализация

**Фаза 2: Backend Integration (Месяц 4-6)**
**Статус****:** Планируется
**Архитектурный стиль:** SPA + REST API + Database
┌─────────────────────────────────────────────────────┐
│                    Frontend                         │
│  ┌───────────────────────────────────────────────┐ │
│  │   React SPA (or keep Vanilla JS)              │ │
│  │   - State management (Zustand)                │ │
│  │   - API client                                │ │
│  │   - Offline support (Service Worker)          │ │
│  └─────────────────┬───────────────────────────── │
└────────────────────┼──────────────────────────────┘
                     │
                     │ REST API / GraphQL
                     │
┌────────────────────▼──────────────────────────────┐
│                  Backend API                       │
│  ┌───────────────────────────────────────────────┐│
│  │   FastAPI (Python)                            ││
│  │   ├─ /auth    - Authentication                ││
│  │   ├─ /users   - User management               ││
│  │   ├─ /lessons - Lesson delivery               ││
│  │   ├─ /stats   - Statistics & analytics        ││
│  │   └─ /ai      - AI recommendations            ││
│  └───────────────────────────────────────────────┘│
│                                                     │
│  ┌───────────────────────────────────────────────┐│
│  │   PostgreSQL                                  ││
│  │   - User data                                 ││
│  │   - Progress tracking                         ││
│  │   - Keystroke history                         ││
│  └───────────────────────────────────────────────┘│
│                                                     │
│  ┌───────────────────────────────────────────────┐│
│  │   Redis                                       ││
│  │   - Session storage                           ││
│  │   - Caching                                   ││
│  │   - Rate limiting                             ││
│  └───────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
         │
         │ Deploy on
         ▼
┌─────────────────────────────────────────────────────┐
│   Cloud Platform (AWS/GCP/DigitalOcean)            │
│   - Docker containers                               │
│   - Managed PostgreSQL                              │
│   - Managed Redis                                   │
└─────────────────────────────────────────────────────┘
**Новые возможности:**
✅ Облачная синхронизация
✅ Мультиустройство
✅ Аутентификация
✅ Расширенная статистика

**Фаза 3: Микросервисы + AI (Месяц 7-12)**
**Статус:** Будущее
**Архитектурный стиль:** Microservices + Event-Driven
┌────────────────────────────────────────────────────────────┐
│                         Frontend                           │
│   React/Next.js + Mobile Apps (iOS/Android)                │
└───────────────────────┬────────────────────────────────────┘
                        │
                        │ API Gateway
                        │
┌───────────────────────▼────────────────────────────────────┐
│                    API Gateway / BFF                        │
│   - Authentication                                          │
│   - Rate limiting                                           │
│   - Request routing                                         │
│   - Response aggregation                                    │
└──┬────────┬─────────┬──────────┬──────────┬────────────────┘
   │        │         │          │          │
   │        │         │          │          │
   ▼        ▼         ▼          ▼          ▼
┌─────┐ ┌──────┐ ┌────────┐ ┌───────┐ ┌─────────┐
│Auth │ │User  │ │Lesson  │ │Stats  │ │AI/ML    │
│Svc  │ │Svc   │ │Svc     │ │Svc    │ │Svc      │
└──┬──┘ └──┬───┘ └───┬────┘ └───┬───┘ └────┬────┘
   │       │         │          │          │
   │       │         │          │          │
   ▼       ▼         ▼          ▼          ▼
┌─────────────────────────────────────────────────┐
│         Message Queue (RabbitMQ/Kafka)          │
│         - Event-driven communication            │
│         - Async processing                      │
└─────────────────────────────────────────────────┘
   │       │         │          │          │
   ▼       ▼         ▼          ▼          ▼
┌─────┐ ┌─────┐  ┌──────┐  ┌──────┐  ┌─────────┐
│Auth │ │User │  │Course│  │Stats │  │Vector   │
│DB   │ │DB   │  │DB    │  │DB    │  │DB       │
│     │ │     │  │      │  │(Time)│  │(AI)     │
└─────┘ └─────┘  └──────┘  └──────┘  └─────────┘
**Микросервисы:**
**Auth Service** - Аутентификация и авторизация
**User Service** - Профили пользователей
**Lesson Service** - Курсы и уроки
**Statistics Service** - Аналитика и метрики
**AI/ML Service** - Персонализация и рекомендации
**Notification Service** - Email, Push, SMS
**Payment Service** - Подписки и платежи

**🏛****️ High-Level архитектура (целевая, v3.0)**
┌─────────────────────────────────────────────────────────────────┐
│                         Client Layer                            │
├─────────────┬─────────────┬──────────────┬─────────────────────┤
│ Web App     │ Mobile iOS  │ Mobile       │ Desktop App         │
│ (React)     │ (Swift)     │ Android      │ (Electron)          │
└─────────────┴─────────────┴──────────────┴─────────────────────┘
              │
              │ HTTPS / WSS (WebSockets для real-time)
              │
┌─────────────▼──────────────────────────────────────────────────┐
│                      CDN Layer (CloudFlare)                     │
│  - Static assets caching                                        │
│  - DDoS protection                                              │
│  - SSL/TLS termination                                          │
└─────────────┬──────────────────────────────────────────────────┘
              │
┌─────────────▼──────────────────────────────────────────────────┐
│                  Load Balancer (HAProxy/NGINX)                  │
│  - Request distribution                                         │
│  - Health checks                                                │
│  - SSL offloading                                               │
└─────────────┬──────────────────────────────────────────────────┘
              │
┌─────────────▼──────────────────────────────────────────────────┐
│                      API Gateway                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Kong / AWS API Gateway / Custom (FastAPI)              │    │
│  │ - Authentication (JWT validation)                      │    │
│  │ - Rate limiting                                        │    │
│  │ - Request transformation                               │    │
│  │ - Logging & monitoring                                 │    │
│  └────────────────────────────────────────────────────────┘    │
└─────┬──────────┬──────────┬──────────┬──────────┬──────────────┘
      │          │          │          │          │
      │          │          │          │          │
┌─────▼────┐ ┌──▼────┐ ┌───▼─────┐ ┌──▼─────┐ ┌─▼────────┐
│ Auth     │ │ User  │ │ Lesson  │ │ Stats  │ │ AI/ML    │
│ Service  │ │ Svc   │ │ Service │ │ Svc    │ │ Service  │
└─────┬────┘ └──┬────┘ └───┬─────┘ └──┬─────┘ └─┬────────┘
      │          │          │          │          │
      │          │          │          │          │
┌─────▼──────────▼──────────▼──────────▼──────────▼──────────┐
│            Message Bus (RabbitMQ / Kafka)                   │
│            - Event publishing/subscribing                    │
│            - Async task processing                          │
└─────────────────────────────────────────────────────────────┘
      │          │          │          │          │
┌─────▼────┐ ┌──▼────┐ ┌───▼─────┐ ┌──▼─────┐ ┌─▼────────┐
│          │ │       │ │         │ │        │ │          │
│ Auth DB  │ │ User  │ │ Course  │ │ Stats  │ │ Vector   │
│ (Postgres│ │ DB    │ │ DB      │ │ DB     │ │ DB       │
│  )       │ │(Post) │ │(Post)   │ │(Influx)│ │(Pinecone)│
│          │ │       │ │         │ │        │ │          │
└──────────┘ └───────┘ └─────────┘ └────────┘ └──────────┘
      │          │          │          │          │
┌─────▼──────────▼──────────▼──────────▼──────────▼──────────┐
│              Cache Layer (Redis Cluster)                    │
│              - Session storage                              │
│              - API response caching                         │
│              - Real-time data                               │
└─────────────────────────────────────────────────────────────┘
      │
┌─────▼───────────────────────────────────────────────────────┐
│              Object Storage (AWS S3 / MinIO)                │
│              - Static assets                                │
│              - User uploads                                 │
│              - Backups                                      │
└─────────────────────────────────────────────────────────────┘
      │
┌─────▼───────────────────────────────────────────────────────┐
│              Monitoring & Logging                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Prometheus   │  │ Grafana      │  │ ELK Stack    │     │
│  │ (Metrics)    │  │ (Dashboards) │  │ (Logs)       │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘

**🧩**** Компоненты системы**
**Frontend компоненты**
frontend/
├── src/
│   ├── components/          # UI компоненты
│   │   ├── Keyboard/       # Виртуальная клавиатура
│   │   ├── TextEditor/     # Текстовый редактор
│   │   ├── Stats/          # Панель статистики
│   │   └── Lessons/        # Компоненты уроков
│   │
│   ├── features/            # Feature modules
│   │   ├── auth/           # Аутентификация
│   │   ├── courses/        # Курсы и уроки
│   │   ├── analytics/      # Аналитика
│   │   └── ai/             # AI функции
│   │
│   ├── services/            # API клиенты
│   │   ├── api.js          # Base API client
│   │   ├── authService.js  # Auth API
│   │   ├── lessonService.js
│   │   └── statsService.js
│   │
│   ├── store/              # State management
│   │   ├── authStore.js
│   │   ├── lessonStore.js
│   │   └── statsStore.js
│   │
│   ├── utils/              # Утилиты
│   │   ├── keyboard.js
│   │   ├── analytics.js
│   │   └── storage.js
│   │
│   └── hooks/              # Custom React hooks
│       ├── useKeyboard.js
│       ├── useTypingSession.js
│       └── useStats.js

**Backend ****компоненты**
backend/
├── services/
│   ├── auth/               # Authentication Service
│   │   ├── api/
│   │   │   ├── routes.py
│   │   │   └── middleware.py
│   │   ├── models/
│   │   │   └── user.py
│   │   ├── services/
│   │   │   ├── jwt_service.py
│   │   │   └── oauth_service.py
│   │   └── tests/
│   │
│   ├── users/              # User Management Service
│   │   ├── api/
│   │   ├── models/
│   │   │   ├── user_profile.py
│   │   │   └── preferences.py
│   │   ├── services/
│   │   └── tests/
│   │
│   ├── lessons/            # Lesson Delivery Service
│   │   ├── api/
│   │   ├── models/
│   │   │   ├── course.py
│   │   │   ├── lesson.py
│   │   │   └── progress.py
│   │   ├── services/
│   │   │   ├── lesson_loader.py
│   │   │   └── progress_tracker.py
│   │   └── tests/
│   │
│   ├── statistics/         # Statistics Service
│   │   ├── api/
│   │   ├── models/
│   │   │   ├── keystroke.py
│   │   │   ├── session.py
│   │   │   └── metrics.py
│   │   ├── services/
│   │   │   ├── aggregator.py
│   │   │   └── analyzer.py
│   │   └── tests/
│   │
│   └── ai/                 # AI/ML Service
│       ├── api/
│       ├── models/
│       │   ├── user_profile.py
│       │   └── recommendations.py
│       ├── ml/
│       │   ├── pattern_analyzer.py
│       │   ├── exercise_generator.py
│       │   └── difficulty_adapter.py
│       └── tests/
│
├── shared/                 # Shared libraries
│   ├── database/
│   │   ├── connection.py
│   │   └── models.py
│   ├── messaging/
│   │   └── rabbitmq.py
│   ├── cache/
│   │   └── redis_client.py
│   └── utils/
│
└── infrastructure/
    ├── docker/
    │   ├── auth.Dockerfile
    │   ├── users.Dockerfile
    │   └── ...
    ├── kubernetes/
    │   ├── deployments/
    │   ├── services/
    │   └── ingress/
    └── terraform/
        ├── aws/
        └── gcp/

**💻**** Технологический стек**
**Frontend**

**Backend**

**AI/ML Stack**

**DevOps & Infrastructure**

**📈**** Стратегия масштабирования**
**Горизонтальное масштабирование**
**Stateless сервисы:**
┌────────────┐
│  Client    │
└─────┬──────┘
      │
      ▼
┌────────────────────────────────┐
│    Load Balancer               │
└──┬─────────┬──────────┬────────┘
   │         │          │
   ▼         ▼          ▼
┌─────┐  ┌─────┐  ┌─────┐
│API-1│  │API-2│  │API-3│  ← Добавляем инстансы
└─────┘  └─────┘  └─────┘
   │         │          │
   └─────────┴──────────┘
             │
             ▼
      ┌─────────────┐
      │  Database   │  ← Single point (пока)
      └─────────────┘
**Масштабирование по метрикам:**
CPU > 70% → +1 инстанс
Memory > 80% → +1 инстанс
Request queue > 100 → +1 инстанс
Response time > 500ms → +1 инстанс

**Вертикальное масштабирование**
**База данных:**
Starter:   2 vCPU, 4GB RAM   → до 10k users
Growth:    4 vCPU, 16GB RAM  → до 100k users
Scale:     8 vCPU, 32GB RAM  → до 500k users
Enterprise: 16 vCPU, 64GB RAM → до 2M users
**Оптимизация запросов:**
Индексы на часто используемых колонках
Партиционирование таблиц (по датам)
Read replicas для аналитики
Connection pooling (PgBouncer)

**Кэширование стратегия**
**Уровни кэша:**
**Browser Cache** (фронтенд)
Static assets: 1 год
API responses: 5 минут
User data: до logout
**CDN Cache** (CloudFlare)
Static files: до изменения
HTML: 1 час
API: не кэшируем
**Application Cache** (Redis)
Session data: 24 часа
Lesson content: 1 час
User stats: 5 минут
Leaderboards: 1 минута
**Database Cache** (PostgreSQL)
Query results: встроенный кэш
Materialized views: для отчетов

**🔒**** Безопасность**
**Аутентификация и авторизация**
**JWT-based auth:**
// Access Token (короткоживущий)
{
  "sub": "user_id",
  "email": "user@example.com",
  "role": "premium",
  "exp": 1728475200,  // 15 минут
  "iat": 1728474300
}

// Refresh Token (долгоживущий)
{
  "sub": "user_id",
  "type": "refresh",
  "exp": 1736250300,  // 30 дней
  "iat": 1728474300
}
**OAuth 2.0 ****поддержка****:**
Google Sign-In
GitHub
Apple ID (для iOS)

**Защита данных**
**Шифрование:**
**В транзите:** TLS 1.3
**В покое:** AES-256
**Passwords:** bcrypt (cost factor 12)
**Sensitive data:** Field-level encryption
**GDPR Compliance:**
// User data structure with privacy in mind
{
  id: "uuid",
  email: "email@example.com",  // PII
  hashedPassword: "...",        // Encrypted
  profile: {
    displayName: "User123",     // Non-PII
    avatar: "url"
  },
  keystrokeData: {
    // Анонимизированные данные для AI
    aggregated: true,
    personalIdentifiers: false
  },
  consents: {
    analytics: true,
    marketing: false,
    dataSharing: false
  },
  gdpr: {
    dataExportUrl: "/api/gdpr/export",
    dataDeleteUrl: "/api/gdpr/delete",
    consentHistory: [...]
  }
}

**Rate Limiting**
**Уровни**** ****защиты****:**
# API Gateway level
@rate_limit(requests=100, window=60)  # 100 req/min
def api_handler():
    pass

# Service level
@rate_limit(requests=1000, window=3600)  # 1000 req/hour per user
def user_action():
    pass

# Critical endpoints
@rate_limit(requests=5, window=60)  # 5 req/min для регистрации
def register():
    pass
**DDoS Protection:**
CloudFlare WAF
IP reputation checking
Challenge pages для подозрительного трафика
Automatic blacklisting

**🔄**** Миграционный путь**
**Фаза 1 → Фаза 2: Добавление Backend**
**Стратегия:** Incremental migration
Шаг 1: Добавить backend параллельно
┌──────────┐
│ Frontend │──┐
└──────────┘  │
              ├──→ New API (optional)
              │
LocalStorage──┘

Шаг 2: Миграция данных
┌──────────┐
│ Frontend │──┐
└──────────┘  │
              ├──→ API (required for new features)
              │   ├─ New: Cloud sync
              │   └─ New: Analytics
              │
LocalStorage──┘   (fallback)

Шаг 3: Полная миграция
┌──────────┐
│ Frontend │────→ API (all operations)
└──────────┘      ├─ Auth
                  ├─ Data
                  └─ AI

LocalStorage: только кэш
**Миграция пользовательских данных:**
// Auto-migration при первом входе
async function migrateLocalData() {
  const localData = localStorage.getItem('userData');
  if (localData && !user.dataMigrated) {
    await api.post('/migrate', {
      localProgress: localData.progress,
      localStats: localData.stats
    });
    user.dataMigrated = true;
  }
}

**Фаза 2 → Фаза 3: Микросервисы**
**Стратегия:** Strangler Fig Pattern
Монолит                  →        Микросервисы

┌────────────────┐              ┌──────────┐
│                │              │ Gateway  │
│   Monolith     │              └────┬─────┘
│                │                   │
│  ┌──────────┐  │              ┌────▼─────┐
│  │ Auth     │  │  →           │ Auth Svc │
│  ├──────────┤  │              └──────────┘
│  │ Users    │  │                   │
│  ├──────────┤  │              ┌────▼─────┐
│  │ Lessons  │  │  →  (постепенно)
│  ├──────────┤  │              │ User Svc │
│  │ Stats    │  │              └──────────┘
│  └──────────┘  │                   │
│                │              ┌────▼─────┐
└────────────────┘              │Lesson Svc│
                                └──────────┘
**Порядок выделения сервисов:**
**Auth Service** (независимый, критичный)
**Stats Service** (много данных, высокая нагрузка)
**AI Service** (специализированный стек)
**Lesson Service** (бизнес-логика)
**User Service** (после стабилизации)

**📊**** Метрики и мониторинг**
**Ключевые метрики**
**Инфраструктура:**
Response time: p50, p95, p99
Error rate: < 0.1%
Uptime: 99.9%
Request rate: req/sec
Database connections
**Бизнес:**
Daily Active Users (DAU)
Lesson completion rate
Average session duration
Conversion rate (free → premium)
Churn rate
**Performance:**
Target SLAs:
- API response time: < 100ms (p95)
- Page load time: < 2s
- Time to interactive: < 3s
- Lesson load time: < 500ms

**🎯**** Ключевые решения**
**Почему FastAPI?**
✅ Высокая производительность (async)
✅ Автоматическая документация (Swagger)
✅ Type hints и валидация (Pydantic)
✅ WebSocket поддержка
✅ Легко масштабируется
**Почему PostgreSQL?**
✅ ACID транзакции
✅ JSON поддержка (для гибких данных)
✅ Отличная производительность
✅ Rich ecosystem (расширения)
✅ Managed services (RDS, Cloud SQL)
**Почему не MongoDB?**
❌ Нужны транзакции (платежи)
❌ Сложные JOIN запросы
❌ Референсная целостность важна
✅ PostgreSQL JSON = 90% MongoDB функционала
**Почему Kubernetes?**
✅ Cloud-agnostic
✅ Автоматическое масштабирование
✅ Self-healing
✅ Индустриальный стандарт
❌ Но: overkill для MVP, добавим в v3.0

**📝**** Следующие шаги**
**Немедленно (эта неделя)**
✅ Утвердить общую архитектуру
[ ] Детализировать Frontend архитектуру
[ ] Детализировать Backend архитектуру
[ ] Спроектировать Database schema
**Краткосрочно (этот месяц)**
[ ] Создать POC backend API
[ ] Настроить CI/CD pipeline
[ ] Подготовить Docker окружение
[ ] Выбрать хостинг провайдера

**Статус:** ✅ Утверждено к реализации
**Следующий документ:** Frontend Architecture Deep Dive
**Дата обновления:** 09 октября 2025


| Компонент | MVP (v1.0) | Target (v2.0+) | Обоснование |
| --- | --- | --- | --- |
| Framework | Vanilla JS | React 18 + TypeScript | Быстрый старт → Масштабируемость |
| State | LocalStorage | Zustand / Redux Toolkit | Простота → Комплексное состояние |
| Styling | Pure CSS | Tailwind CSS | Полный контроль → Быстрая разработка |
| Build | None | Vite | Статика → Оптимизация |
| Testing | Manual | Jest + Vitest | - → Автоматизация |
| Types | None | TypeScript | JS → Типобезопасность |


| Компонент | MVP (v1.0) | Target (v2.0+) | Обоснование |
| --- | --- | --- | --- |
| Framework | - | FastAPI (Python) | Нет бэкенда → Быстрый Python API |
| Database | LocalStorage | PostgreSQL 15+ | Браузер → ACID транзакции |
| Cache | - | Redis 7+ | - → Производительность |
| Queue | - | RabbitMQ / Kafka | - → Async обработка |
| Search | - | Elasticsearch | - → Полнотекстовый поиск |
| Storage | - | AWS S3 / MinIO | - → Масштабируемое хранилище |


| Компонент | Level 1 | Level 2 | Level 3 |
| --- | --- | --- | --- |
| Analysis | JavaScript | Python scikit-learn | TensorFlow / PyTorch |
| LLM | - | - | OpenAI / Anthropic API |
| Vector DB | - | - | Pinecone / Weaviate |
| Feature Store | - | Redis | Feast |
| ML Ops | - | MLflow | Kubeflow |


| Компонент | MVP (v1.0) | Target (v2.0+) |
| --- | --- | --- |
| Hosting | Netlify/Vercel | AWS/GCP/DigitalOcean |
| Containers | - | Docker + Docker Compose |
| Orchestration | - | Kubernetes |
| CI/CD | GitHub Actions | GitHub Actions + ArgoCD |
| Monitoring | - | Prometheus + Grafana |
| Logging | Console | ELK Stack / CloudWatch |
| APM | - | Sentry / New Relic |
| IaC | - | Terraform |
